import 'package:flutter/material.dart';

class Notes_Teacher extends StatefulWidget {
  const Notes_Teacher({Key? key}) : super(key: key);

  @override
  _Notes_TeacherState createState() => _Notes_TeacherState();
}

class _Notes_TeacherState extends State<Notes_Teacher> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
